# npm 설정 파일 - 모든 환경에서 동일한 설정 사용

# peer dependency 경고를 무시하고 설치 진행
legacy-peer-deps=true

# 패키지 잠금 파일 사용 (보안 및 일관성)
package-lock=true

# npm 7+ 호환성
lockfile-version=3

# 설치 시 진행률 표시
progress=true

# 취약점 감사 자동 실행
audit=true

# 캐시 설정 (Windows 호환성)
cache-max=86400000 